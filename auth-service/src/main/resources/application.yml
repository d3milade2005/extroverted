server:
  port: 8082

spring:
  application:
    name: auth-service

logging:
  level:
    root: INFO
    com.event: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always